Description:
    Install the views for thredded so that the parent application can customize them.

Example:
    rails generate thredded:views

    This will create:
        app/views/thredded/*/**
        app/views/layouts/thredded.html.erb
