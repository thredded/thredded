<% [:success, :error, :notice, :alert].each do |key| %>
  <% if flash[key] %>
    <div class="flash-message flash-message--<%= key %>" id="flash">
      <%= flash[key] %>
    </div>
  <% end %>
<% end %>
